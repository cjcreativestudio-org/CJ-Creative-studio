# 10 — PadelVision: The Newest Build

**Pillar:** P1 Speed Builds · **Length:** ~30s · **Audience:** both
**Assets:** the PadelVision build (site/product + repo). ⚠️ Fill the capture slots below from the real project before filming — this script deliberately avoids inventing product details.

## Pre-fill (do this before filming)

- [ ] `HERO_MOMENT` — the single most impressive 3 seconds of the PadelVision UI (hero animation, live data view, court footage integration — whatever it actually is)
- [ ] `KEY_FEATURE` — the one feature a stranger instantly understands
- [ ] `BUILD_TIME` — the honest elapsed build time
- [ ] `ONE_LINER` — what PadelVision is, in ≤8 words

## Hook (first 1.5s)

**Spoken + on-screen:**
> "Newest client build: PadelVision. Nothing to live — watch."

Visual: `HERO_MOMENT` immediately, full screen.

## Shot list

| # | Time | Shot |
|---|---|---|
| 1 | 0–3s | OBS: `HERO_MOMENT` — the finished thing, best angle first |
| 2 | 3–7s | OBS: hour-zero shot — empty repo/terminal scaffold (re-record if original wasn't captured) |
| 3 | 7–14s | OBS timelapse: build progression — editor + localhost refreshes, punch-ins on the UI coming together |
| 4 | 14–20s | OBS: `KEY_FEATURE` demoed in real time, no cuts |
| 5 | 20–24s | OBS: deploy → live. Vercel dashboard Ready state, then the production URL loading |
| 6 | 24–30s | 📱 Phone in hand using the live product. End card |

## Voiceover

> "Newest client build: PadelVision. Nothing to live — watch.
> `ONE_LINER` — that's the brief we got.
> Hour zero. Agents scaffold, we design.
> *(over the feature demo)* This is `KEY_FEATURE` — the bit that makes it work as a business, not just a website.
> `BUILD_TIME` from first commit to live.
> Every client build gets posted. This is number three."

## On-screen text

- 0s: **"NEW CLIENT BUILD: PADELVISION"**
- 4s: "HOUR 0"
- 8s: "agents build → we direct"
- 15s: *(name `KEY_FEATURE` in ≤5 words)*
- 21s: "LIVE"
- 25s (end card): **"Your build could be next — DM 'SPRINT'"**

## Caption

> Client build #3: PadelVision — `ONE_LINER`.
>
> Same pipeline as always: AI agents scaffold and iterate, we direct design, Vercel ships it. `BUILD_TIME` from empty folder to live.
>
> We post every build, start to finish. Want yours on this feed? DM "SPRINT".

## Hashtags

#webdesignuk #padel #buildinpublic #webdev #uksmallbusiness #websitedesign

## CTA

**DM "SPRINT"**

## Notes

- If PadelVision is a product (video/analytics) rather than a marketing site, lean the edit toward the product demo (shot 4 grows, shot 3 shrinks) — product demos outperform code timelapses with normie audiences.
- Confirm the client is happy being featured before posting (TEMPLATE.md checklist).
